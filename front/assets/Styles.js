import { StyleSheet } from "react-native"

export const basicStyles = StyleSheet.create({
    text: {
        fontFamily: 'Arial',
        color: 'white',
    },

    header1: {
        fontSize: 30,
        textAlign: 'center',
        fontWeight: 'bold',
        marginTop: 10,
        marginBottom: 20,
    },

    lable: {
        paddingLeft: 20,
        marginBottom: 5,
    },

    buttonText: {
        textAlign: 'center',
        fontWeight: 'bold',
        color: 'black',
    }
});
